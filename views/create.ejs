<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TMS Blog - Create Blog</title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="font-sans bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen">
  <!-- Navbar -->
  <nav class="bg-gradient-to-r from-primary to-purple-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <div class="flex-shrink-0">
          <a href="/home" class="text-2xl font-bold text-white tracking-tight">TMS Blog</a>
        </div>
        <div class="flex space-x-2 sm:space-x-4">
          <a href="/home" class="text-white hover:bg-primary/80 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Home</a>
          <a href="/create" class="text-white hover:bg-primary/80 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Create Blog</a>
          <a href="/api-docs" class="text-white hover:bg-primary/80 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">API Docs</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Create Blog Form -->
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="bg-white rounded-xl shadow-md p-6 sm:p-8">
      <h1 class="text-3xl sm:text-4xl font-bold text-gray-800 mb-6 leading-tight">Create a New Blog</h1>
      <form action="/create" method="POST" enctype="multipart/form-data" class="space-y-6">
        <!-- Title -->
        <div>
          <label for="title" class="block text-sm font-medium text-gray-700 mb-1">Title</label>
          <input type="text" id="title" name="title" required class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-colors duration-200" placeholder="Enter blog title">
        </div>

        <!-- Author -->
        <div>
          <label for="author" class="block text-sm font-medium text-gray-700 mb-1">Author (Optional)</label>
          <input type="text" id="author" name="author" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-colors duration-200" placeholder="Enter author name">
        </div>

        <!-- Content -->
        <div>
          <label for="content" class="block text-sm font-medium text-gray-700 mb-1">Content</label>
          <textarea id="content" name="content" required rows="6" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-colors duration-200" placeholder="Write your blog content here..."></textarea>
        </div>

        <!-- Media Upload -->
        <div>
          <label for="media" class="block text-sm font-medium text-gray-700 mb-1">Upload Media (Up to 5 files, Images or Videos)</label>
          <input type="file" id="media" name="media" multiple accept="image/*,video/*" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-colors duration-200">
          <p class="mt-1 text-sm text-gray-500">Max 5 files. Supported formats: JPG, PNG, MP4, etc.</p>
        </div>

        <!-- Submit Button -->
        <div class="text-center">
          <button type="submit" class="bg-primary text-white px-6 py-3 rounded-lg shadow-md hover:bg-primary/80 transition-colors duration-200">Publish Blog</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>