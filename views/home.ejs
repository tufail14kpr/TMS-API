<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TMS Blog - Home</title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="font-sans bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen">
  <!-- Navbar -->
  <nav class="bg-gradient-to-r from-indigo-600 to-purple-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <div class="flex-shrink-0">
          <a href="/home" class="text-2xl font-bold text-white tracking-tight">TMS Blog</a>
        </div>
        <div class="flex space-x-2 sm:space-x-4">
          <a href="/home" class="text-white hover:bg-indigo-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Home</a>
          <a href="/create" class="text-white hover:bg-indigo-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Create Blog</a>
          <a href="/api-docs" class="text-white hover:bg-indigo-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">API Docs</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="bg-gradient-to-r from-indigo-600 to-purple-600 text-white py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl sm:text-5xl font-extrabold tracking-tight">Welcome to TMS Blog</h1>
      <p class="mt-4 text-lg sm:text-xl opacity-90">Explore insightful stories, tutorials, and more from our community.</p>
    </div>
  </div>

  <!-- Blog List -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <% if (blogs && blogs.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% blogs.forEach(blog => { %>
          <div class="bg-white rounded-xl shadow-md overflow-hidden transform hover:scale-105 transition-transform duration-300">
            <% if (blog.media && blog.media.length > 0 && blog.media[0].resource_type === 'image') { %>
              <img src="<%= blog.media[0].url %>" alt="Blog Media" class="w-full h-48 sm:h-56 object-cover">
            <% } else { %>
              <div class="w-full h-48 sm:h-56 bg-gray-200 flex items-center justify-center">
                <span class="text-gray-500 text-sm">No Image Available</span>
              </div>
            <% } %>
            <div class="p-6">
              <h2 class="text-xl font-semibold text-gray-800 mb-2 line-clamp-2"><%= blog.title %></h2>
              <div class="text-gray-500 text-sm mb-4 flex items-center space-x-2">
                <span>By <%= blog.author || 'Anonymous' %></span>
                <span class="text-gray-300">•</span>
                <span><%= new Date(blog.createdAt).toLocaleDateString() %></span>
              </div>
              <p class="text-gray-600 line-clamp-3 mb-4"><%= blog.content %></p>
              <a href="/blogs/<%= blog.slug %>" class="inline-block text-indigo-600 font-medium hover:text-indigo-800 transition-colors duration-200">Read More →</a>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="text-center py-12">
        <p class="text-gray-600 text-lg sm:text-xl mb-6">No blogs found. Be the first to create one!</p>
        <a href="/create" class="inline-block bg-indigo-600 text-white px-6 py-3 rounded-lg shadow-md hover:bg-indigo-700 transition-colors duration-200">Create a Blog</a>
      </div>
    <% } %>
  </div>
</body>
</html>