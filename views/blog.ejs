<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TMS Blog - <%= blog.title %></title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="font-sans bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen">
  <!-- Navbar -->
  <nav class="bg-gradient-to-r from-primary to-purple-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <div class="flex-shrink-0">
          <a href="/home" class="text-2xl font-bold text-white tracking-tight">TMS Blog</a>
        </div>
        <div class="flex space-x-2 sm:space-x-4">
          <a href="/home" class="text-white hover:bg-primary/80 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Home</a>
          <a href="/create" class="text-white hover:bg-primary/80 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">Create Blog</a>
          <a href="/api-docs" class="text-white hover:bg-primary/80 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">API Docs</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Blog Content -->
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="bg-white rounded-xl shadow-md p-6 sm:p-8">
      <h1 class="text-3xl sm:text-4xl font-bold text-gray-800 mb-4 leading-tight"><%= blog.title %></h1>
      <div class="text-gray-500 text-sm sm:text-base mb-6 flex items-center space-x-2">
        <span>By <%= blog.author || 'Anonymous' %></span>
        <span class="text-gray-300">•</span>
        <span><%= new Date(blog.createdAt).toLocaleDateString() %></span>
      </div>
      <div class="prose prose-lg text-gray-700 mb-8 max-w-none">
        <p><%= blog.content %></p>
      </div>
      <% if (blog.media && blog.media.length > 0) { %>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <% blog.media.forEach(item => { %>
            <% if (item.resource_type === 'image') { %>
              <img src="<%= item.url %>" alt="Blog Media" class="w-full h-64 object-cover rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200">
            <% } else if (item.resource_type === 'video') { %>
              <video controls src="<%= item.url %>" class="w-full h-64 object-cover rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200"></video>
            <% } %>
          <% }) %>
        </div>
      <% } %>
      <div class="mt-8">
        <a href="/home" class="inline-block text-primary font-medium hover:text-primary/80 transition-colors duration-200">← Back to Home</a>
      </div>
    </div>
  </div>
</body>
</html>